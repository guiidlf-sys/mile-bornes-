--!strict

local GameConstants = {
	MIN_PLAYERS = 2,
	MAX_PLAYERS = 4,
	HAND_SIZE = 6,
	TARGET_DISTANCE = 1000,
	SPEED_LIMIT_CAP = 50,
	LOBBY_COUNTDOWN_SECONDS = 10,
	COUP_FOURRE_WINDOW_SECONDS = 8,

	SCORE_TRIP_COMPLETED = 300,
	SCORE_PER_SAFETY = 100,
	SCORE_ALL_SAFETIES_BONUS = 300,
	SCORE_SHUTOUT = 500,
	SCORE_COUP_FOURRE = 300,

	MAX_LOG_ENTRIES = 40,
}

return GameConstants
